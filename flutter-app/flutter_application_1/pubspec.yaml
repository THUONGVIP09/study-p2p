name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  intl: ^0.19.0
  shared_preferences: ^2.1.1
  flutter_webrtc:  any
  web_socket_channel: ^3.0.3
  permission_handler: ^11.0.0
  path_provider: ^2.1.1      # For local file storage
  network_info_plus: ^5.0.0  # For getting device IP

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  

flutter:
  uses-material-design: true
  assets:
    - assets/images/

